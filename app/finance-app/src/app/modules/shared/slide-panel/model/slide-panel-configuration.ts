export interface SlidePanelConfiguration {
  name: {
    enable: boolean;
    label: string;
  };
  alias: {
    enable: boolean;
    label: string;
  };
  isExternal: {
    enable: boolean;
    label: string;
  };
  utfIcon: {
    enable: boolean;
    label: string;
  };
  button: {
    label: string;
  };
}
